[package]
name = "cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
lexer = { path = "../lexer" }
parser = { path = "../parser" }
runtime = { path = "../runtime" }
syntax = { path = "../syntax" }
