[package]
name = "test262"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
crux = { path = "../crux" }
jit = { path = "../jit" }
parser = { path = "../parser" }
runtime = { path = "../runtime", features = ["workers"] }
syntax = { path = "../syntax" }
